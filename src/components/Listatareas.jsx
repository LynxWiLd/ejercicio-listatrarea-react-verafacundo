import ListGroup from 'react-bootstrap/ListGroup';
import Items from './Items';

const Listatareas = () => {
  return (
    <ListGroup>
      <Items></Items>
    </ListGroup>
  );
}

export default Listatareas;
